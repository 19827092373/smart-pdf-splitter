name: Deploy to Aliyun

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy via SCP
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SERVER_IP }}
          username: root
          key: ${{ secrets.SERVER_SSH_KEY }}
          source: "."
          target: "/www/wwwroot/pdf.teacherlab.cn"
          rm: false
          overwrite: true
          strip_components: 0
      
      - name: Clean up unnecessary files on server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_IP }}
          username: root
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd /www/wwwroot/pdf.teacherlab.cn
            rm -rf .github
            rm -rf .git
            rm -rf .gitignore
            rm -rf .claude
            rm -rf *.md
            rm -rf deploy.sh
            rm -rf sync.sh
            rm -rf update.sh

      - name: Install dependencies and deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_IP }}
          username: root
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd /www/wwwroot/pdf.teacherlab.cn
            
            # 检查 Python 版本
            PYTHON_VERSION=$(python3 --version 2>&1 | awk '{print $2}' | cut -d. -f1,2)
            echo "当前 Python 版本: $PYTHON_VERSION"
            
            # 检查并升级 pip
            pip3 install --upgrade pip setuptools wheel --quiet
            
            # 安装系统依赖（Poppler）
            if ! command -v pdftoppm &> /dev/null; then
              echo "📦 安装 Poppler..."
              apt-get update -qq
              apt-get install -y poppler-utils --quiet
            fi
            
            # 安装项目依赖（使用 pip3，不指定版本限制）
            echo "📦 安装项目依赖..."
            pip3 install streamlit pypdf pdf2image requests pandas --quiet || {
              echo "⚠️  使用兼容版本安装..."
              pip3 install streamlit pypdf pdf2image requests pandas --upgrade --quiet
            }
            
            # 检查部署方式并更新
            if [ -f "docker-compose.yml" ] && command -v docker-compose &> /dev/null; then
              echo "🐳 使用 Docker 方式部署..."
              docker-compose down || true
              docker-compose build --no-cache
              docker-compose up -d
              
              # 等待服务启动
              sleep 5
              
              # 检查服务状态
              docker-compose ps
              docker-compose logs --tail=50
            elif [ -f "/etc/systemd/system/pdf-splitter.service" ]; then
              echo "🖥️  使用 systemd 方式部署..."
              if [ -d "venv" ]; then
                source venv/bin/activate
                pip install -r requirements.txt --quiet
              fi
              sudo systemctl restart pdf-splitter
              sudo systemctl status pdf-splitter --no-pager
            else
              echo "📦 使用 Streamlit 直接运行..."
              # 停止可能正在运行的进程
              pkill -f "streamlit run app.py" || true
              sleep 2
              
              # 后台运行 Streamlit
              nohup streamlit run app.py --server.port=8501 --server.address=0.0.0.0 > streamlit.log 2>&1 &
              sleep 5
              
              # 检查进程
              if ps aux | grep -v grep | grep "streamlit run app.py" > /dev/null; then
                echo "✅ Streamlit 已启动"
                ps aux | grep streamlit | grep -v grep
              else
                echo "❌ Streamlit 启动失败，查看日志:"
                tail -50 streamlit.log
                exit 1
              fi
            fi
            
            echo "✅ 部署完成！"

      - name: Health check
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_IP }}
          username: root
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            # 检查服务是否正常运行
            sleep 3
            cd /www/wwwroot/pdf.teacherlab.cn
            if curl -f http://localhost:8501/_stcore/health > /dev/null 2>&1; then
              echo "✅ 服务健康检查通过"
            else
              echo "⚠️  服务健康检查失败，请查看日志"
              if [ -f "docker-compose.yml" ] && command -v docker-compose &> /dev/null; then
                docker-compose logs --tail=100
              elif [ -f "streamlit.log" ]; then
                tail -50 streamlit.log
              fi
              echo "请检查服务是否正常运行"
              exit 1
            fi
